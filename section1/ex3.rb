# this prints a string
puts "I will now count my chickens:"

# this prints a string with an interpolated equation inside.
puts "Hens #{25.0 + 30.0 / 6.0}"

# same here.
puts "Roosters #{100.0 - 25.0 * 3.0 % 4.0}"

# this is another string
puts "Now I will count the eggs:"

# this is just the equation with no string
puts 3.0 + 2.0 + 1.0 - 5.0 + 4.0 % 2.0 - 1.0 / 4.0 + 6.0

# another string
puts "Is it true that 3 + 2 < 5 - 7?"

# another equation
puts 3.0 + 2.0 < 5.0 - 7.0

# an interpolated equation inside of a string
puts "What is 3 + 2? #{3.0 + 2.0}"

# interpolated equation inside a string
puts "What is 5 - 7? #{5.0 - 7.0}"

# a string
puts "Oh, that's why it's false."

# a string
puts "How about some more."

# a string with an interpolated equation inside
puts "Is it greater? #{5.0 > -2.0}"

# a string with an interpolated equation inside
puts "Is it greater or equal? #{5.0 >= -2.0}"

# a string with an interpolated equation inside
puts "Is it less or equal? #{5.0 <= -2.0}"
